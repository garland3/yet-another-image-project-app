edit meta-data, both for projects and images make tables for meta-data for images and project, and allow editing them
add comments to images
-- different users' can make comments. 
define and edit classes for a project
-- a project can have N image classes. like plane, train, car, etc., ... 
draw bounding boxes on images
-- an image can have N bounding boxes, and each bounding box can be assigned to a class.
-- a bounding box can have a comment, and a class.
allow defining N catogies to a image, like, defetive, good, bad, etc.


add ui or logging to the docker setup. 
